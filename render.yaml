services:
  - type: web
    name: family-hub-api
    runtime: node
    rootDir: app-dev
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: NODE_ENV
        value: production
